import { JsHrefDirective } from './js-href.directive';

describe('JsHrefDirective', () => {
  it('should create an instance', () => {
    const directive = new JsHrefDirective();
    expect(directive).toBeTruthy();
  });
});
